# Archived Projects

This folder contains retired or paused project snapshots.

Code here is not maintained, not part of the active npm workspace, not part of normal repo validation, and not a source of truth for current product direction.

The maintained runnable products are:

- `gemma-desktop`
- `gemma-cli`

Shared product contracts and reusable behavior belong in `gemma-sdk/`.
