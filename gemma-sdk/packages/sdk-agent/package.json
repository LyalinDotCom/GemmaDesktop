{
  "name": "@gemma-sdk/agent",
  "version": "0.1.0",
  "description": "Gemma-optimized agent loop, model providers, and workspace tools for Gemma products.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --root ../.. src/*.test.ts src/providers/*.test.ts src/tools/*.test.ts",
    "typecheck": "tsc -b"
  },
  "files": [
    "dist",
    "!dist/**/*.test.*",
    "!dist/providers/ollamaContext.*",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^2.0.45",
    "ai": "^6.0.174"
  }
}
