# @gemma-sdk/agent

Internal support package for `gemma-cli`.

This package contains the agent loop, model providers, model profiles, content handling, skills, and workspace tools used by the Gemma CLI npm package. It is published so the CLI can install cleanly from npm.

This is an unofficial project and is not affiliated with Google, DeepMind, Gemma, Ollama, or LM Studio. It is provided as-is, without warranties. Use at your own risk.
